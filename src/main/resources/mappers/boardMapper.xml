<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardMapper">
	<insert id="insert">
	   INSERT INTO mp_board(bno, title, content, writer) VALUES((SELECT NVL(MAX(bno),0) + 1 FROM mp_board), #{title}, #{content}, #{writer})
    </insert>
    
    <select id="list" resultType="kr.co.vo.BoardVO">
    	SELECT bno, title, writer, regdate FROM mp_board ORDER BY bno DESC
    </select>
</mapper>
