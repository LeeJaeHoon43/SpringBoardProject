<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="boardMapper">
	<insert id="insert">
	   INSERT INTO mp_board(bno, title, content, writer) VALUES((SELECT NVL(MAX(bno),0) + 1 FROM mp_board), #{title}, #{content}, #{writer})
    </insert>
    
    <select id="listPage" resultType="kr.co.vo.BoardVO" parameterType="kr.co.vo.Criteria">
		SELECT bno, title, content, writer, regdate FROM (SELECT bno, title, content, writer, regdate, ROW_NUMBER() OVER(ORDER BY bno DESC) AS rNum FROM mp_board WHERE 1=1 <include refid="search"></include>) mp WHERE rNum BETWEEN #{rowStart} AND #{rowEnd} ORDER BY bno DESC
	</select>
    
    <select id="read" parameterType="int" resultType="kr.co.vo.BoardVO">
    	SELECT bno, title, content, writer, regdate FROM mp_board WHERE bno = #{bno}
    </select>
    
    <update id="update" parameterType="kr.co.vo.BoardVO">
    	UPDATE mp_board SET title = #{title}, content = #{content} WHERE bno = #{bno}
    </update>
    
    <delete id="delete" parameterType="int">
    	DELETE FROM mp_board WHERE bno = #{bno}
    </delete>
    
    <select id="listCount" parameterType="kr.co.vo.SearchCriteria" resultType="int">
		SELECT COUNT(bno) FROM mp_board WHERE 1=1 <include refid="search"></include> AND bno > 0
	</select>
	
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 't'.toString()">AND title LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'c'.toString()">AND content LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'w'.toString()">AND writer LIKE '%' || #{keyword} || '%'</if>
			<if test="searchType == 'tc'.toString()">AND (title LIKE '%' || #{keyword} || '%') or (content LIKE '%' || #{keyword} || '%')</if>
		</if>
	</sql>
	
</mapper>